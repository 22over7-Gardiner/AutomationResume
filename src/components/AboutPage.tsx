import React from'react';import{SectionShell}from'./SectionShell';import{ABOUT_FOCUS,ABOUT_INTRO}from'../content';export function AboutPage():React.ReactElement{return <SectionShell eyebrow="About Me" title="Engineering that reaches the floor" intro={ABOUT_INTRO} placeholderNote={false}><div className="space-y-6"><div><h3 className="text-sm font-semibold">Strengths</h3><ul className="mt-3 flex flex-wrap gap-2">{ABOUT_FOCUS.map(f=><li key={f} className="rounded-full border border-border bg-surface px-3 py-1 text-xs font-medium">{f}</li>)}</ul></div><div className="rounded border border-border bg-surface p-5"><h3 className="text-sm font-semibold">How I work</h3><p className="mt-2 text-sm leading-relaxed text-foregroundMuted">I like problems that sit between disciplines — where mechanical, electrical, controls, and software all have to agree. I troubleshoot to root cause, prototype quickly, validate with data, and lean on Lean and 6 Sigma to build processes that operators and maintenance teams can actually run. Just as important, I document and teach what I learn so the whole team gets stronger.</p></div></div></SectionShell>}
