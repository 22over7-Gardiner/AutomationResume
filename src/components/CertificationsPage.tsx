import React from'react';import{SectionShell}from'./SectionShell';import{CERTIFICATIONS,PROFICIENCIES}from'../content';export function CertificationsPage():React.ReactElement{return <SectionShell eyebrow="Certifications" title="Certifications & technical training" intro="Formal certifications alongside the tools, processes and systems I work in day to day." placeholderNote={false}><div className="space-y-8"><div><h3 className="text-sm font-semibold">Certifications</h3><ul className="mt-3 grid gap-3 sm:grid-cols-2">{CERTIFICATIONS.map(c=><li key={c.name} className="rounded border border-border bg-surface p-4"><div className="flex justify-between gap-3"><h4 className="text-sm font-semibold">{c.name}</h4><span className="shrink-0 rounded-full bg-primary/10 px-2 py-.5 text-[11px] font-semibold text-primary">{c.status}</span></div><p className="mt-2 text-xs text-foregroundMuted">{c.issuer}{c.year!=='—'?` · ${c.year}`:''}</p></li>)}</ul></div><div><h3 className="text-sm font-semibold">Technical proficiencies</h3><div className="mt-3 grid gap-3 sm:grid-cols-2">{PROFICIENCIES.map(g=><div key={g.group} className="rounded border border-border bg-surface p-4"><h4 className="text-xs font-semibold uppercase tracking-wide text-primary">{g.group}</h4><ul className="mt-2 flex flex-wrap gap-1.5">{g.items.map(i=><li key={i} className="rounded border border-border bg-surfaceAlt px-2 py-.5 text-[11px] text-foregroundMuted">{i}</li>)}</ul></div>)}</div></div></div></SectionShell>}
