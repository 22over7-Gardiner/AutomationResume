import React from'react';import{Moon,Sun}from'lucide-react';import{OrbitNavigation}from'./OrbitNavigation';import type{SectionId}from'../types';
export function LandingPage({onSelect,theme,onToggleTheme}:{onSelect:(id:SectionId)=>void;theme:'light'|'dark';onToggleTheme:()=>void}):React.ReactElement{const dark=theme==='dark';return <main className="relative flex h-full min-h-0 flex-col overflow-hidden"><header className="flex shrink-0 items-center justify-between px-5 pt-5"><span className="font-display text-sm font-semibold">D. Gardiner</span><button type="button" onClick={onToggleTheme} aria-label={dark?'Switch to light mode':'Switch to dark mode'} className="flex h-11 w-11 items-center justify-center rounded-full border border-border bg-surface focus-visible:ring-2 focus-visible:ring-primary">{dark?<Sun className="h-5 w-5"/>:<Moon className="h-5 w-5"/>}</button></header><div className="min-h-0 flex-1 px-3 py-2"><OrbitNavigation onSelect={onSelect}/></div><footer className="shrink-0 px-5 pb-5 text-center"><p className="text-xs tracking-wide text-foregroundMuted sm:text-sm">Automation · Robotics · Machine Vision · Industry 4.0</p><p className="mt-1 text-[11px] text-foregroundMuted/80 sm:hidden">Tap an icon to explore</p></footer></main>}
