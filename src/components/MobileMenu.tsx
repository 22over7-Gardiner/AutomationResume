import React from'react';import{X}from'lucide-react';import{SECTIONS}from'../navigation';import type{SectionId}from'../types';
export function MobileMenu({open,current,onClose,onNavigate}:{open:boolean;current:SectionId;onClose:()=>void;onNavigate:(id:SectionId)=>void}):React.ReactElement|null{const ref=React.useRef<HTMLDivElement>(null);React.useEffect(()=>{if(!open)return;const f=(e:KeyboardEvent)=>{if(e.key==='Escape')onClose()};document.addEventListener('keydown',f);ref.current?.querySelector<HTMLButtonElement>('button[data-first]')?.focus();return()=>document.removeEventListener('keydown',f)},[open,onClose]);if(!open)return null;return <div className="fixed inset-0 z-50"><div className="absolute inset-0 bg-foreground/25" onClick={onClose}/><div ref={ref} role="dialog" aria-modal="true" aria-label="Site navigation" className="absolute right-0 top-0 flex h-full w-full flex-col bg-surface shadow-xl sm:w-[360px]"><div className="flex items-center justify-between border-b border-border px-5 py-4"><span className="font-display font-semibold">Navigate</span><button onClick={onClose} aria-label="Close menu" className="flex h-11 w-11 items-center justify-center rounded-full"><X className="h-5 w-5"/></button></div><nav className="flex-1 overflow-y-auto p-3"><ul className="flex flex-col gap-1">{SECTIONS.map((s,i)=>{const I=s.Icon,c=s.id===current;return <li key={s.id}><button data-first={i===0||undefined} aria-current={c?'page':undefined} onClick={()=>onNavigate(s.id)} className={`flex w-full min-h-[44px] items-center gap-3 rounded px-3 py-2.5 text-left ${c?'bg-primary/10 text-primary':'hover:bg-surfaceAlt'}`}><span className="flex h-9 w-9 items-center justify-center rounded-full border border-border bg-surface"><I className="h-4 w-4"/></span><span><span className="block text-sm font-medium">{s.label}</span><span className="block text-xs text-foregroundMuted">{s.tagline}</span></span></button></li>})}</ul></nav></div></div>}
