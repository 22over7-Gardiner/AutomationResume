import React from 'react';import type{NavSection}from'../types';
export function OrbitItem({section,active,onSelect,onInteractStart,onInteractEnd}:{section:NavSection;active:boolean;onSelect:(id:NavSection['id'])=>void;onInteractStart:(id:string,k:'hover'|'focus'|'press')=>void;onInteractEnd:(k:'hover'|'focus'|'press')=>void}):React.ReactElement{const{Icon,label,short,id}=section;return <button type="button" aria-label={label} onClick={()=>onSelect(id)} onMouseEnter={()=>onInteractStart(id,'hover')} onMouseLeave={()=>onInteractEnd('hover')} onFocus={()=>onInteractStart(id,'focus')} onBlur={()=>onInteractEnd('focus')} onPointerDown={()=>onInteractStart(id,'press')} onPointerUp={()=>onInteractEnd('press')} onPointerCancel={()=>onInteractEnd('press')} onPointerLeave={()=>onInteractEnd('press')} className="no-select group flex touch-manipulation flex-col items-center gap-1.5 rounded-full p-1 outline-none"><span className={`flex h-12 w-12 min-h-[44px] min-w-[44px] items-center justify-center rounded-full border bg-surface text-primary shadow-sm transition-transform duration-200 group-focus-visible:ring-2 group-focus-visible:ring-primary ${active?'scale-110 border-primary bg-primary text-accentForeground':'border-border'}`}><Icon className="h-5 w-5"/></span><span className={`whitespace-nowrap rounded-full bg-surface/80 px-2 py-0.5 text-[11px] font-medium tracking-wide transition-opacity sm:text-xs ${active?'opacity-100 text-primary':'text-foregroundMuted opacity-70 sm:opacity-0 sm:group-hover:opacity-100 sm:group-focus-visible:opacity-100'}`}>{short}</span></button>}
