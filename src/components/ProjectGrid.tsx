import React from'react';import type{ProjectEntry}from'../types';export function ProjectGrid({projects}:{projects:ProjectEntry[]}):React.ReactElement{return <div className="grid gap-4 sm:grid-cols-2">{projects.map(p=><article key={p.title} className="flex flex-col rounded border border-border bg-surface p-5"><span className="text-[11px] font-semibold uppercase tracking-wide text-primary">{p.category}</span><h3 className="mt-1 font-display font-semibold">{p.title}</h3><p className="mt-2 flex-1 text-sm leading-relaxed text-foregroundMuted">{p.summary}</p><ul className="mt-4 flex flex-wrap gap-1.5">{p.tags.map(t=><li key={t} className="rounded border border-border bg-surfaceAlt px-2 py-.5 text-[11px] text-foregroundMuted">{t}</li>)}</ul></article>)}</div>}
